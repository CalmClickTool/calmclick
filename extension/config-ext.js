/* Extension uses packaged rules only (no remote host permission).
 * New patterns ship with extension updates from the Chrome Web Store.
 * The public website can still auto-update its own rules feed.
 */
window.CALMCLICK_CONFIG = {
  version: "1.1.1",
  /* Empty = do not fetch remote rules inside the extension */
  rulesFeedUrl: "",
  githubUrl: "https://github.com/CalmClickTool/calmclick"
};
